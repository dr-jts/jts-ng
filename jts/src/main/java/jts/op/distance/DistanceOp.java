package jts.op.distance;

import jts.geom.Geometry;
import jts.op.GeometryValueResult;
import jts.op.Operation;

public class DistanceOp implements Operation {
	public DistanceOp(Geometry geom) {
		
	}
	
	public GeometryValueResult execute(Geometry geom) {
		return null;
	}
}
